apiVersion: v1
kind: Service
metadata:
  name: {{ include "movie-picker.fullname" . }}
  labels:
    {{- include "movie-picker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "movie-picker.selectorLabels" . | nindent 4 }}
