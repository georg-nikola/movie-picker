<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme; Compose overrides this at runtime via MoviePickerTheme -->
    <style name="Theme.MoviePicker" parent="android:Theme.Material.NoTitleBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
